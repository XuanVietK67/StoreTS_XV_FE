import { getStores } from "@/services/store.service";

const StorePage = async () => {
    // const stores = await getStores();
    // console.log("check store: ", stores)
    return(
        <div>
            store
        </div>
    )
};

export default StorePage;
